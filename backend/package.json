{
  "name": "suhrit-backend",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/web-api/handler.js",
  "scripts": {
    "build": "npm run build:web-api",
    "build:web-api": "esbuild src/lambdas/web-api/handler.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/web-api/handler.js --sourcemap",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "mongodb": "^6.14.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.13.10",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.2"
  }
}
